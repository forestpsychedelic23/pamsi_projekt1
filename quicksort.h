
class QuickSort{
	
	template<class T>
	T Divide(T tab[], T low, T high);
	
	template<class T>
	void Replace(T tab[], T i, T j);
	
	public:
	template <class T>
	void Sort(T tab[], T low, T high);
};

